<%= form_for @tag do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.count, "error") %> prohibited this owner from being saved:</h2>

      <ul>
      <% @tag.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
  <% end %>
     
     <%= f.text_area :items, :placeholder => "Items" %>
     <%= f.submit "Create Tags" %>
<% end %>